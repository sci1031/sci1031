# L'analyse de données spatiales {#analyse}


```{r module9, echo = FALSE, results='hide', warning = FALSE, message = FALSE, eval = TRUE}
source("root_path.R")
root = root_path()
module<- paste(root, "/Module9", sep = "")
```


```{r setup, echo=FALSE,results='hide', warning = FALSE, message = FALSE, eval = TRUE}
path=paste(root, "/Structure_test/sci1031", sep = "")
chunkoptions=paste(path,"/bin/chunk-options.R", sep = "")
source(chunkoptions)
```
L'objectif principal de ce module est

À la fin de ce module vous saurez:

-
-
-

Vous utiliserez les librairies suivantes:

- 
-

Vous apprendrez à utiliser les fonctions suivantes:

- 
-

Dans la section Leçon, vous utiliserez des données XYXYX

Dans la section Exercice, vous utiliserez XXXXX

```{r load-libraries, echo = FALSE, results='hide', warning = FALSE, message = FALSE}
#library(raster)
library(rgdal)
library(ggplot2)
library(dplyr)
library(sf)
library(svglite) ## J'ai pas besoin de ça pour créer les figures, mais pour les inclure dans le markdown, oui.
library(ggpubr)
```


## Leçon

### Distance

-	Distance entre des points, distance entre des cellules, matrice de distance, distance pour les coordonnées angulaires.

### Configuration spatiale

-	Proximité, plus proche voisin, frontière, superficie, connectivité, inclusion, zones tampons, matrice de résistance.

### Autocorrélation spatiale

-	Covariance et correlation, autocorrelation temporelle et spatiale, indice de Moran.




## Exercice

